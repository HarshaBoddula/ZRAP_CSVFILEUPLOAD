projection;
strict ( 2 );
use draft;

define behavior for ZC_P2_POITEM //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action UploadCSV;
  use action calcStockAmountDeep;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}